# ESPHome build cache (multiple GB)
.esphome/
build/
.pio/
.pioenvs/

# Local secrets — never committed. Every builder must create their
# own `firmware/source/secrets.yaml` from the tracked
# `secrets.yaml.example` template (see instructions inside).
secrets.yaml
*.PRIVATE

# Python venv used by the local ESPHome toolchain
venv/
.venv/

# Editor & OS
.DS_Store
.idea/
.vscode/
*.swp
__pycache__/
